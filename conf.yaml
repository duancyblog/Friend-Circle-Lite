spider_settings:
  enable: true
  json_url: "https://blog.allen2030.com/friend.json" # 替换为步骤 1 中部署的 JSON 地址
  article_count: 5 # 每个友链保留 5 篇最新文章
  merge_result:
    enable: false # 暂不合并其他 JSON，单源爬取足够

# 邮箱订阅功能（可选，无需则保持 disable）
rss_subscribe:
  enable: false # 关闭则无需配置 SMTP
smtp:
  enable: false # 无需邮件推送则关闭
